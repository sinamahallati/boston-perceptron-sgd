# Boston Perceptron (SGDRegressor)

Minimal single-neuron linear regression on the Boston Housing dataset using `StandardScaler + SGDRegressor`.
Includes an interactive 3D scatter + fitted plane.

## Run
```bash
jupyter notebook
